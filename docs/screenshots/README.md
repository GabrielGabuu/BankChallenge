# Screenshots

Coloque aqui os prints da aplicação com os seguintes nomes:

| Arquivo | Tela |
|---|---|
| `login.png` | Tela de login (`/login`) |
| `register.png` | Tela de registro (`/register`) |
| `dashboard.png` | Dashboard principal (`/`) |
| `accounts.png` | Listagem de contas (`/accounts`) |
| `transactions.png` | Histórico de transações (`/transactions`) |

## Dica de tamanho
- Resolução recomendada: **1280×800** ou **1440×900**
- Pode usar a ferramenta de captura do Windows (`Win + Shift + S`) ou extensão do Chrome
